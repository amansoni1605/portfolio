import Skills from "@/components/Skills";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Skills | Aman Soni",
};

export default function SkillsPage() {
  return <Skills />;
}
