import Contact from "@/components/Contact";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact | Aman Soni",
};

export default function ContactPage() {
  return <Contact />;
}
